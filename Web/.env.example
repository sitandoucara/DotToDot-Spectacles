# Copy this file to .env.local and fill in the anon key.
# Both values are safe in a browser bundle as long as the photos table
# only exposes a SELECT policy to anon (which is what you already set up).

VITE_SUPABASE_URL=paste-your-supabase-url-here
VITE_SUPABASE_ANON_KEY=paste-your-anon-key-here
